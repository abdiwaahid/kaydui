---
import { Prism } from "@astrojs/prism";
import { categories } from "../lib/code";
import { baseUrl } from "../utils/utils";
const { id, name, slug, code, category } = Astro.props;

const componentCategory = categories.filter((c) => c.id === category).at(0);

---

<div
  class={`h-64 p-0 bg-white dark:bg-gray-800 rounded-xl shadow-xs border border-gray-200 dark:border-gray-700 hover:shadow-sm transition-all duration-200 flex flex-col ${componentCategory?.slug}`}
>
  <div class="grow flex items-center justify-center rounded-t-lg overflow-hidden">
    <img src={`${baseUrl}/thumbnails/${slug}.png`} alt="" class="object-contain block" />
  </div>
  <a class=" block p-6" href={`${baseUrl}/components/${slug}`}>
    <h3 class="font-semibold text-lg mb-1 text-gray-900 dark:text-white">
      {name}
    </h3>
    <p class="text-sm text-gray-600 dark:text-gray-300">
      {componentCategory?.name}
    </p>
  </a>
</div>
